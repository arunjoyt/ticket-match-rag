# Proposed Architecture (retired)

Everything this document once proposed — the Postgres cache, the RQ worker, the `retrieval/indexing.py` choke point, and the Frappe bridge app serving Matches inside Helpdesk's own agent UI — is implemented. See [ARCHITECTURE.md](ARCHITECTURE.md) for the current system, and [ADR 0006](adr/0006-background-compute-and-helpdesk-integration.md) / [ADR 0007](adr/0007-accept-match-cache-staleness.md) for the decision record, per this repo's convention of ADRs as history rather than living documents.

Kept as a stub, not deleted, so links from those ADRs and from `git log` still resolve.
